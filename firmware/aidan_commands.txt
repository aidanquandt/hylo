
TWR (Updated for new architecture):

uwb.set.address 0x0001
twr.set.mode anchor

uwb.set.address 0x0002  
twr.set.mode tag
twr.req.range 0x0001
twr.get.status  # Shows current mode and transition status


TWR AUTO-RANGING (Updated for new architecture):

uwb.set.address 0x0001
twr.set.mode anchor

uwb.set.address 0x0002
twr.set.mode tag
twrmgr.req.start
twrmgr.get.status
twrmgr.req.stop

UWB PORT STATS:

uwb.get.stats

BEACON SEND:

uwb.set.address 0x0001
beacon.req.ping 0x0002

uwb.set.address 0x0002
beacon.req.ping 0x0001

uwb.set.address 0x0002
beacon.req.ping.delayed 0x0001

STOP WATCH STUFF:

stopwatch.get.0
stopwatch.get.1
stopwatch.get.all

DATALOGGER STUFF:

datalogger.get.stats